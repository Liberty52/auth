server:
  port: 8080

spring:
  application:
    name: auth
  profiles:
    active: dev
  config:
    import:
      - optional:configserver:http://3.34.181.222:8888/
      - classpath:aws-config-sample.yml
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: on
      path: /h2-console
  cloud:
    config:
      label: main

logging:
  level:
    root: info

frontend:
  url: http://43.201.26.253/redirect?accessToken=%s&refreshToken=%s