server:
  port: 8081

spring:
  application:
    name: auth
  profiles:
    active: dev
  config:
    import:
      - optional:configserver:http://config-server.liberty52.com:8888/
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: on
      path: /h2-console
  cloud:
    config:
      label: main

logging:
  level:
    root: info